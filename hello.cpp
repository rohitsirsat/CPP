#include <iostream>

// #include --> preprocessor directive (compiler directive) 
// there are different types of preprocessor directives like #include , #define etc

// <iostream> --> header file (standard library) include iostream header file to use cout, cin etc

using namespace std;

// using namespace std; --> this is a global directive that tells the compiler to use the standard namespace. To avoid the conflict with other namespaces/custom namespaces

// we can also build our own namespaces and use them. ex: namespace myNamespace { }

// there is a framework called Qt (Qt is a cross-platform application and UI framework) 

// Eigen is a linear algebra library for C++. 


int main() {
    cout << "Hello C++ from Rohit";
}